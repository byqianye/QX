# AGENTS.md

## 项目开发规范（Codex 目标模式）

本项目采用 Codex Goal 驱动开发模式，每次只允许执行一个 Goal。

---

## 核心规则

### 1. 修改原则
- 修改前必须阅读相关 Spike 文档与现有测试
- 一次只允许完成一个 Goal
- 禁止无关重构
- 禁止扩大兼容性声明
- 禁止伪造第三方源能力（如真实影视源、解析能力等）

---

### 2. Spider 与引擎规则
- JVM-native Spider ≠ Android DEX 兼容
- JS / JVM / Python / DEX 必须严格区分
- 不得混用运行时假设

---

### 3. 测试规则
- 新功能必须有稳定测试
- 不允许跳过测试 / 降低断言 / mock 掩盖失败
- Electron 生命周期必须验证子进程退出

---

### 4. 构建与发布
- 不允许自动 push
- 不允许在未通过 E2E 的情况下打包发布
- 所有修改必须可回滚

---

### 5. 目标模式规则
- 每个 Goal 必须包含：
  - 状态
  - 依赖
  - 范围
  - 验收标准
  - 验证命令
- 完成后必须输出：
  - 修改文件列表
  - 测试结果
  - 未完成事项
  - 风险说明

---

## 当前阶段

- Stage 0：Spike 0–19 已完成
- Stage 1：G20–G78 目标模式进行中
