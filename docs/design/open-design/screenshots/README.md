# Open Design screenshots

本次运行通过 Open Design MCP 生成设计合同、规格和 handoff 文件；运行没有生成 HTML/SVG 原型，也没有导出可信截图。项目工作区没有用户提供的品牌参考图或真实海报，因此不能把预览空白、普通占位图或截图参考 README 渲染结果冒充产品截图。

复现方式：使用同一套 `design-system.md` token、`page-specs.md` 的页面顺序和三种窗口验收条件运行本地 Vue renderer；截图如需验收，应来自实际 renderer/packaged app，并注明 `1280×720`、`1440×900` 或 `1920×1080` 的尺寸和状态。此处禁止伪造截图。
