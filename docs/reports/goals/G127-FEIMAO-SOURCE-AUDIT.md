# G127 肥猫默认源专项适配报告

验证日期：2026-09-05
源地址：http://xn--z7x900a.net/

## 结论

当前配置共 39 个站点：37 个已落入项目现有专用适配器或声明式/受控合同边界，1 个 QuickJS 外部脚本源，1 个尚无专用合同的源（`csp_Hxq`/韩圈）。因此不能说“全部源都已适配”。

## 清单审计

完整机器可读清单见 `artifacts/feimao-source-adaptation-audit.json`。

- 已有适配边界：37
- QuickJS 外部脚本：1（儿童）
- 待专项合同：1（韩圈，`csp_Hxq`）

已适配并不等于每个上游当前可用。AppGet/AppQi、360、Wwys、SaoHuo、Gz360、Kanqiu 等仍受 TLS、WAF、登录、动态签名或上游状态影响，必须以逐源 canary 结果为准。

## 本次实施

本轮完成了默认源配置的真实获取、解码、39 个站点盘点和分类产物生成。没有对 `csp_Hxq` 猜测接口，也没有把 QuickJS 脚本伪装成 Rust HTTP 源。

## 后续专项

下一步应单独为 `csp_Hxq` 获取真实接口合同，再验证读取、搜索、详情、媒体解析和播放；若依赖动态签名或 HTML/脚本，应新增隔离适配器。儿童源继续保持 QuickJS 边界，不执行远程脚本以外的替代逻辑。
