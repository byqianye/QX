# 配置导入

QX影视支持从用户提供的本地文件、URL 或 JSON 内容导入媒体配置。导入流程分为“加载、预览、确认”三步；预览阶段不会直接把未经确认的配置写入正式来源。

只导入你控制或获授权访问的地址。确认前检查来源名称、接口地址和启用项；遇到超时、取消或格式错误，先修正输入再重试。

不要把包含密码、Cookie、Authorization 或服务 Token 的原始配置提交到 issue、聊天或日志中。导入失败时只提供脱敏后的错误代码、版本和系统架构。
